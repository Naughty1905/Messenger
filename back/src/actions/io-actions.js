const DISCONNECT = 'disconnect';
const CONNECTION = 'connection';
const JOIN = 'join';
const MESSAGE = 'messege';
const SEND_MESSAGE = 'sendMessege';
const CHECK_READ_MESSAGE = 'CHECK_READ_MESSAGE'

module.exports = {
  DISCONNECT,
  CONNECTION,
  JOIN,
  MESSAGE,
  SEND_MESSAGE,
  CHECK_READ_MESSAGE
}
